# ─────────────────────────────────────────────────────────────────────────────
# Local Development Environment Variables
# Copy this file and rename to .env if needed, or export these in your shell.
# DO NOT commit real credentials to Git - this file has safe dev defaults only.
# ─────────────────────────────────────────────────────────────────────────────

# PostgreSQL Connection
DB_URL=jdbc:postgresql://localhost:5432/productdb
DB_USERNAME=devuser
DB_PASSWORD=devpass

# App
SERVER_PORT=8080
SPRING_PROFILES_ACTIVE=default
